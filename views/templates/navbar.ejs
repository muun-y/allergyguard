<nav class="fixed top-0 z-50 w-full bg-white border-b">
    <div class="px-3 py-3 lg:px-5 lg:pl-3">
        <div class="flex md:ml-16 md:justify-between">

            <!-- Logo -->
            <div class="md:hidden flex items-center justify-center flex-1 md:flex-none ml-8 md:ml-0">
                <a href="/" class="flex ms-2">
                    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor"
                        stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-6 h-6">
                        <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                        <line x1="16" y1="2" x2="16" y2="6"></line>
                        <line x1="8" y1="2" x2="8" y2="6"></line>
                        <line x1="3" y1="10" x2="21" y2="10"></line>
                        <text x="12" y="18" font-family="Arial, sans-serif" font-size="5" text-anchor="middle"
                            fill="currentColor">♡</text>
                    </svg>
                </a>
            </div>

            <% if (!user) { %>
                <!-- Not logged in -->
                <div class="hidden md:flex md:items-center md:justify-between w-full">
                    <div class="flex flex-auto justify-center font-semibold">Home</div>
                    <div class="flex items-end">
                        <a href="/login">
                            <button type="button" class="bg-black text-white px-4 py-1 text-center rounded-lg">
                                Log in
                            </button>
                        </a>
                    </div>
                </div>

                <% } else { %>
                    <!-- Logged in -->
                    <div class="hidden md:flex items-center justify-center w-full relative">
                        <div class="grow flex justify-center">
                            <button id="dropdownNavbarLink" data-dropdown-toggle="dropdownNavbar"
                                class="flex items-center justify-center w-full py-2 px-3 text-gray-900 rounded font-semibold">
                                For you
                                <div class="ml-2 border-2 rounded-full w-6 h-6 flex justify-center items-center">
                                    <svg class="w-2.5 h-2.5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg"
                                        fill="none" viewBox="0 0 10 6">
                                        <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"
                                            stroke-width="2" d="m1 1 4 4 4-4" />
                                    </svg>
                                </div>
                            </button>
                        </div>

                        <!-- Dropdown -->
                        <div id="dropdownNavbar" class="absolute top-full mt-2 z-50 hidden font-normal bg-white divide-y divide-gray-100 
                        rounded-lg shadow w-44 border">
                            <div class="py-1">
                                <a href="/logout" class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100">Log
                                    out</a>
                            </div>
                        </div>
                    </div>
                    <% } %>
        </div>
    </div>
</nav>

<%- include("sidebar") %>